#include <unistd.h>
#include <curses.h>
#include "main.h"

void boxIncrementer()
{
	if (tick = 90000){
		kittens += (foodCans);
		i = 0;
	}
}

void *charGetter(void *char_void_ptr)
{
	while (buttonClick != 'q' ) {
		buttonClick = getch();
	}
}

