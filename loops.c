#include <unistd.h>
#include <curses.h>
#include "main.h"

void boxIncrementer()
{
	if (i = 10000){
		if (foodCans > 0) {
			kittens += (foodCans * 1.5);
		}
		i = 0;
	}
}

void *charGetter(void *char_void_ptr)
{
	while (buttonClick != 'q' ) {
		buttonClick = getch();
	}
}

